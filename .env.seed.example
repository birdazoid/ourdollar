# Credentials for scripts/seed.ts ONLY. Copy to .env.seed (gitignored) and fill in.
# The service_role key is a SECRET admin key that bypasses row-level security.
# It must NEVER be committed and NEVER be exposed to the app (note: no EXPO_PUBLIC_ prefix,
# so Expo will not bundle it). Find it in Supabase: Project Settings > API keys > Legacy /
# service_role (or the new "Secret key").

SUPABASE_URL=https://cxrjlqzvfvubvcrftihq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-or-secret-key
